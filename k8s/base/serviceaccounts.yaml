# ========================================
# Service Account for External Secrets Operator
# ========================================
# This service account is used by the SecretStore to access AWS Secrets Manager
# Note: backend-sa and frontend-sa are defined in their respective deployment files

apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::478266318018:role/openmarket-external-secrets-role
